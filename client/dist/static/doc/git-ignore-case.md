# Git的大小写敏感
*Haowen in 2016-12-08*

最近在项目中新建文件时都用的大写开头的，但是有一个文件一不小心打成了小写，事后很久才发现，于是赶紧把它改回来，结果再后来再去修改这个文件，然后上传到服务器时发生了很诡异的事情：文件打包后并没有发生变化，排查后发现是服务器上有了两个文件，一个是Agreement.vue，另一个是agreement.vue。

查阅资料后，使用

```git config core.ignorecase false```

```git rm <> <>```

将文件重新命名，并提交覆盖
