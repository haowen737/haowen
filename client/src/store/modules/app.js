const state = {
  sideBarPosition: ''
}

const mutations = {
  setSideBarPosition (state, sideBarPosition) {
    state.sideBarPosition = sideBarPosition
  }
}

export default {
  state,
  mutations
}
